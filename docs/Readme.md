# Docs

This project uses Sphinx for generating its documentation.

Install Sphinx:
    
    $ pip install Sphinx

And to build the documentation just run:

    $ cd docs/
    $ make html

Then open the `docs/_build/html/index.html` file in a browser.
